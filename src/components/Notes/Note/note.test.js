// import React from "react";
// // enzyme is used to write unit test for  isolated components
// import { configure, shallow } from "enzyme";
// import adapter from "enzyme-adapter-react-16";
// import Note from "./Note";
// // what shallow does -->it renders the component with all its content but the content isn't deeply rendered .
// configure({ adapter: new adapter() });
// // describe("<  ---console logs --/>", () => {});
// describe("<NavigationItems/>", () => {
//   it("should render two <navigationItems/> elements if not authenticated ", () => {
//     const wrapper = shallow(<NavigationItems />);
//     //     expect("expect nothing and you will never disappointed");
//     expect(wrapper.find(NavigationItem)).toHaveLength(2);
//   });
//   //alow us to write one individual test also takes two arguments
// });
